{% extends "backoffice.html.twig" %}

{% block body %}
<h1>Gestion catégories</h1>

{{form(form)}}

<table class= "table table-dark table-striped">
<thead>
<tr>
<th>id</th>
<th>Titre</th>
<th>Actions</th>
</tr>
</thead>
<tbody>
{% for category in categories %}
<tr>
<td>{{ category.id}}</td>
<td>{{ category.title}} </td>
<td>
<a href="{{path('editCategory', {'id' : category.id}) }}" class="mr-2"><i class="fas fa-2x fa-edit"></i></a>
<a href="{{path('deleteCategory', {'id' : category.id}) }}" class="m1-2 text-warning"><i class="fas fa-2x fa-trash"></i></a> 
</td>
</tr>
{% endfor %}
</tbody>
</table>

{% endblock %} 

{% block title %}
{% endblock %}